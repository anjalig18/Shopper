[build]
  base = "client"
  command = "npm install && CI=false npx react-scripts build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
